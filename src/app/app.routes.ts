import { StudentsComponent } from './students/students.component';
import { Routes } from '@angular/router';

export const routes: Routes = [
  { path: '', component: StudentsComponent }, // Setting StudentsComponent as the initial one
];

